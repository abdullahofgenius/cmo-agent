# Optional: when omitted, the app uses its deterministic local strategy engine.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Public product URL in production.
NEXT_PUBLIC_APP_URL=http://localhost:3000
